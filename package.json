{
  "name": "smart-contract-utils",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "npm version patch && npm run build && npm publish",
    "format": "prettier --write \"./**/*.{ts,js,json,md}\" --ignore-path ./.prettierignore",
    "increment:patch": "npm version patch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4"
  },
  "devDependencies": {
    "del-cli": "^5.0.0",
    "keyv": "^4.5.2",
    "prettier": "^2.8.4",
    "typescript": "^4.9.5"
  }
}
